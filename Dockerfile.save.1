# Use a lightweight web server image
FROM nginx:alpine

# Copy all project files (HTML, CSS, JS) to nginx's default folder
COPY . /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Start nginx in the foreground
CMD ["nginx", "-g", "daemon off;"]

